name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Build + test matrix — see swift-institute/.github/.github/workflows/swift-ci.yml
  ci:
    uses: swift-institute/.github/.github/workflows/swift-ci.yml@main
    with:
      cache-key-prefix: swift63-spm-v1
      enable-private-repos: true
    secrets:
      PRIVATE_REPO_TOKEN: ${{ secrets.PRIVATE_REPO_TOKEN }}

  # DocC umbrella-catalog pipeline per [DOC-019a] — see
  # swift-institute/.github/.github/workflows/swift-docs.yml
  docs:
    uses: swift-institute/.github/.github/workflows/swift-docs.yml@main
    with:
      umbrella-module: PDF_HTML_Rendering
      umbrella-display-name: "PDF HTML Rendering"
      umbrella-bundle-id: "swift-pdf-html-render.PDF-HTML-Rendering"
      umbrella-docc-path: "Sources/PDF HTML Rendering/PDF HTML Rendering.docc"
      exclude-modules: "PDF_HTML_Rendering_Test_Support"
      enable-private-repos: true
    secrets:
      PRIVATE_REPO_TOKEN: ${{ secrets.PRIVATE_REPO_TOKEN }}
