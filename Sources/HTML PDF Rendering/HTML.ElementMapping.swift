// HTML.ElementMapping.swift

import PDF_Rendering
import HTML_Renderable

extension HTML {
    /// Utilities for HTML to PDF mapping.
    ///
    /// This enum provides utility functions for the HTML-to-PDF conversion process.
    /// The main conversion is now handled by `HTMLToPDFConvertible` and `PDF.Converted`.
    public enum ElementMapping {
        // Style parsing utilities are in HTML.ElementMapping.Style.swift
    }
}
